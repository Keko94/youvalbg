{{ form_start(form, {'attr': {'id': 'discutea_forum_post'}}) }}
      {{ form_widget(form.content, {'attr': {'class': 'nk-summernote form-control', 'rows': 16}}) }}
    <div class="form-group" style="display: none">
      <div class="pull-right">
        {% if form.preview is defined %}
        {{ form_widget(form.preview, {'attr': {'class': 'btn btn-default btn-sm'}}) }}
        {% endif %}
      </div>
    </div>
    <div class="nk-gap-1"></div>
    <button type="submit" class="nk-btn nk-btn-rounded nk-btn-color-white">{{ 'discutea.forum.btn.submit'|trans }}</button>
{{ form_end(form) }}
